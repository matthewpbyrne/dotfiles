##### ------------------------------------------------------------------
##### Plugins: baseline shared across profiles
##### ------------------------------------------------------------------

# Reset then build plugin list via append so older tmux reliably keeps all entries.
set -gu @plugin

# Plugin manager
set -g @plugin 'tmux-plugins/tpm'

# Baseline sane defaults
set -ga @plugin ' tmux-plugins/tmux-sensible'
